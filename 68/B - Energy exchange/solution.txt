n, k = map(int,input().split())nums = list(map(int,input().split())) def checker(d):    needed = 0    given = 0    for i in range(len(nums)):        if nums[i] < d:            needed += d - nums[i]        elif nums[i] > d:            y = nums[i] - d            given += y - (y* k)/100    return given >= needed low = 0high = max(nums) while high - low > 10 **-6:    middle = low + (high - low)/2     if checker(middle):        low = middle    else:        high = middleprint(high)