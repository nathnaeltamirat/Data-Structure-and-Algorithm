n, k = map(int,input().split())accumlators = list(map(int,input().split())) def checker(a):    needed = 0     transferred = 0        #making them equal     for i in accumlators:        if i < a:            needed += a - i        else:            transferred += (i - a) * (1 - (k/100))    return transferred >= needed low = 0high = max(accumlators) while high - low > 10**-6:    middle = low + (high - low)/2    if checker(middle):        low = middle    else:        high = middleprint(high)