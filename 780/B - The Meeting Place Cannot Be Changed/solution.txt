n = int(input())pos = list(map(int,input().split()))speed = list(map(int,input().split()))def checker(t):    max_left = float('-inf')    min_right = float('inf')    for p,s in zip(pos,speed):        left = p - (s * t)        right = p + (s * t)        max_left = max(left,max_left)        min_right = min(min_right,right)    return max_left <= min_right low = 0high = max(pos) - min(pos)while high - low > 10 ** -6:    middle = low + (high - low)/2    if checker(middle):        high = middle    else:        low = middleprint(low)