from collections import defaultdict,dequet = int(input())def bfs(graph,indegree,n):    q = deque([node for node in indegree if indegree[node] == 1])    if n == 1:        return 0    count = 0    removed = 0    while q:        length = len(q)        if count == k:            break        for _ in range(length):            node = q.popleft()            removed += 1            for neigh in graph[node]:                indegree[neigh] -= 1                if indegree[neigh] == 1:                    q.append(neigh)        count += 1    return n - removedfor _ in range(t):    input()    n, k = map(int,input().split())    graph = defaultdict(list)    indegree = defaultdict(int)    for _ in range(n-1):        a, b = map(int,input().split())        graph[a].append(b)        graph[b].append(a)        indegree[a] += 1        indegree[b] += 1    ans = bfs(graph,indegree,n)    print(ans)