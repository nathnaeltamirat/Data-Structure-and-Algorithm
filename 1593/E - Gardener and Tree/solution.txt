from collections import defaultdict,dequet = int(input())def bfs(graph,indegree,nodes,k):    total = len(nodes)    if total == 1:        indegree[1] += 1    q = deque([node for node in nodes if indegree[node] == 1])        order = []    steps = removed =  0    while q and steps < k:        steps += 1        n = len(q)        leafes = []        for _ in range(n):            node = q.popleft()            removed += 1            order.append(node)            for neigh in graph[node]:                indegree[neigh] -= 1                if indegree[neigh] == 1:                    leafes.append(neigh)        q.extend(leafes)    return max(0,total - removed)    for _ in range(t):    input()    n, k = map(int,input().split())    graph = defaultdict(list)    indegree = defaultdict(int)    for _ in range(n-1):        a, b = map(int,input().split())        graph[a].append(b)        graph[b].append(a)        indegree[a] += 1        indegree[b] += 1    res = bfs(graph,indegree,range(1,n+1),k)    print(res)