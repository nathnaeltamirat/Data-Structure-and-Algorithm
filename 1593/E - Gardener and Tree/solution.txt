from collections import defaultdict,dequet = int(input())def bfs(graph,indegree,nodes,final):    q = deque([node for node in nodes if indegree[node] == 1])     count = 0    removed = 0    while count != final and q:        count += 1        n = len(q)        removed += n        for _ in range(n):            node = q.popleft()            for neigh in graph[node]:                indegree[neigh] -= 1                if indegree[neigh] == 1:                    q.append(neigh)    # print("len nodes ",len(nodes))    return len(nodes) -removed for _ in range(t):    input()    n, op = map(int,input().split())    graph = defaultdict(list)    indegree = defaultdict(int)     for _ in range(n-1):        a, b = map(int,input().split())        graph[a].append(b)        graph[b].append(a)        indegree[a] += 1        indegree[b] += 1    if n <= 2:        print(0)        continue    print(bfs(graph,indegree,range(1,n+1),op))